<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <!-- Add the Tailwind CSS CDN link -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body >
  <nav class="bg-blue-500 p-4 flex items-center justify-between">
    <div class="text-white text-lg font-bold">SMVITM</div>
    <div class="space-x-10 mr-8">
        <!-- <a href="#" class="text-white">About</a>
        <a href="#" class="text-white">Gallery</a> -->
        <a href="/" class="text-white">HOME</a>
    </div>
</nav>
  <div class="bg-gray-100 h-screen flex flex-col items-center justify-center">
    <div class="max-w-md w-full p-4 md:p-[5vw] bg-white rounded-md shadow-md">
        <h1 class="text-2xl font-semibold mb-4">Login</h1>
        
        <% if (messages.error) { %>
            <p class="text-red-500 mb-4"><%= messages.error %></p>
        <% } %>

        <form action="/login" method="POST">

            <div class="mb-4">
                <label for="email" class="block text-gray-700 text-sm font-bold mb-2">Email</label>
                <input type="email" id="email" name="email" class="w-full border rounded-md py-2 px-3" required>
            </div>

            <div class="mb-4">
                <label for="password" class="block text-gray-700 text-sm font-bold mb-2">Password</label>
                <input type="password" id="password" name="password" class="w-full border rounded-md py-2 px-3" required>
            </div>

            <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded-md hover:bg-blue-700 focus:outline-none focus:shadow-outline-blue active:bg-blue-800">
                Login
            </button>

        </form>
        <div class="flex justify-center align-center">
          <!-- <a href="/register">Register</a> -->
        </div>
    </div>
</div>
</body>
</html>
